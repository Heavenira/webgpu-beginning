{
    "scripts": {
        "build": "esbuild src/app/main.ts --outfile=dist/main.js --target=es2022 --minify --bundle --loader:.wgsl=text"
    },
    "dependencies": {
        "@webgpu/types": "^0.1.34"
    },
    "devDependencies": {
        "@types/node": "^20.3.2"
    }
}
